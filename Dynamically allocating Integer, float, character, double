// Dynamically allocating Integer, float, character, double

1> Write a program that:

1.1> Dynamically allocates a single double variable.
1.2> Asks the user to input a value for the variable.
1.3> Prints the value of the variable with two decimal places.
1.4> Deallocates the variable.


#include<iostream>
using namespace std;

int main(){
    int *x;
    x=new int;
    cout<<"Enter a number: ";
    cin>>*x;
    cout<<*x;
    delete x;
    return 0;
}

------------------------------------------------------------------------------------------------------------------
2> Write a program that:

2.1> Dynamically allocates a single char variable.
2.2> Asks the user to input a character for the variable.
3.1> Prints the ASCII value of the character.
3.1> Deallocates the variable.


#include<iostream>
#include<iomanip>
using namespace std;

int main(){
    double *f;
    f = new double;
    cout << "Enter a floating point number: ";
    cin >> *f;
    cout << fixed << setprecision(2) << "You entered: " << *f << endl;
    delete f;
    return 0;
}

---------------------------------------------------------------------------------------------------------------
3> Write a program that:

3.1> Dynamically allocates a single char variable.
3.2> Asks the user to input a character for the variable.
3.1> Prints the ASCII value of the character.
3.1> Deallocates the variable.


#include<iostream>
#include<iomanip>
using namespace std;

int main(){
    float *f;
    f = new float;
    cout << "Enter a floating point number: ";
    cin >> *f;
    cout << fixed << setprecision(2) << "You entered: " << *f << endl;
    delete f;
    return 0;
}
